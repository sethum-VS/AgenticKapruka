# Ralph progress log
# One block per iteration: decisions, files changed, next PRD hint.

Session: 2026-06-07 | Branch: ralph/sprint-1

## PRD-001 — Initialize Python project with pyproject.toml
- Created pyproject.toml with hatchling build, runtime deps (FastAPI, uvicorn, gunicorn, pydantic v2, jinja2, redis, neo4j, zep-python, mcp, langgraph, google-genai, google-cloud-aiplatform, networkx, ragas) and dev extras (pytest, pytest-asyncio, httpx, playwright, pytest-playwright, ruff, mypy).
- Pinned compatible version ranges; verified `pip install -e '.[dev]'` resolves cleanly in Python 3.13 venv.
- Added baseline [tool.ruff] and [tool.mypy] sections plus pytest asyncio config.
- Added empty app/, lib/, graphs/ package stubs matching hatch wheel packages for editable install and mypy targets.
Files: pyproject.toml, app/__init__.py, lib/__init__.py, graphs/__init__.py
Next: PRD-002 FastAPI application factory (app/main.py create_app).

## PRD-002 — FastAPI application factory
- Implemented `create_app()` in app/main.py with module-level `app` for uvicorn; no CORSMiddleware (same-origin HTMX).
- Lifespan in app/lifespan.py sets app.state.redis/neo4j/zep to None on startup with graceful close on shutdown; real clients land in PRD-008/012/013.
- Route stubs: health (/health), chat, cart, checkout, partials; GET / returns 307 redirect to /chat.
- Unit tests: root redirect, /chat stub, /health stub (tests/unit/test_main.py).
- Verified: uvicorn app.main:app, curl -L / → 200 via /chat.
Files: app/main.py, app/lifespan.py, app/routes/{chat,cart,checkout,health,partials}.py, tests/unit/test_main.py
Next: PRD-003 Pydantic Settings (app/config.py).

## PRD-003 — Pydantic Settings and environment validation
- Added `app/config.py` with pydantic-settings `Settings` loading REDIS_URL, NEO4J_URI/USER/PASSWORD, ZEP_API_KEY, GOOGLE_API_KEY, GCP_PROJECT_ID/LOCATION, KAPRUKA_MCP_URL, SESSION_SECRET.
- Field validators: URL schemes for redis/neo4j/mcp; non-empty strip for production secrets; SESSION_SECRET min 32 chars.
- `get_settings()` via `@lru_cache` singleton; explicit `pydantic-settings` dep in pyproject.toml.
- Unit tests: `test_settings_loads_from_env` plus rejection cases for short session secret and empty API key.
Files: app/config.py, tests/unit/test_settings.py, pyproject.toml
Next: PRD-004 .env.example and .gitignore for secrets.

## PRD-004 — Environment example file
- Created `.env.example` documenting all Settings fields with commented local vs Cloud Run guidance.
- Documented Memorystore VPC connector requirement (`--vpc-connector`, `--vpc-egress=private-ranges-only`) and Secret Manager mapping for Cloud Run deploy.
- Confirmed `.gitignore` already excludes `.env` and `.env.*` while allowing `.env.example`; placeholders only, no real secrets.
Files: .env.example
Next: PRD-005 Jinja2 template environment (app/templating.py).

## PRD-005 — Jinja2 template environment
- Added `app/templating.py` with cached `get_templates()` dependency, custom `jinja2.Environment` (`auto_reload` when `APP_ENV` != production), and `format_currency` filter stub for PRD-056.
- Created `templates/base.html` shell: HTMX 2.0.4, htmx-ext-sse 2.2.2, Alpine.js 3.14.8 CDN scripts; `hx-ext="sse"` on body; title/content blocks.
- Unit tests: filter stub, singleton dependency, `TemplateResponse` renders base.html with expected script tags.
Files: app/templating.py, templates/base.html, tests/unit/test_templating.py
Next: PRD-006 Tailwind CSS standalone CLI build (static/css/input.css).

## PRD-006 — Tailwind CSS standalone CLI build
- Added `static/css/input.css` with `@tailwind` directives, base body styles, and component classes (`btn-primary`, `btn-secondary`, `card-commerce`).
- Added `tailwind.config.js` with Kapruka red (`kapruka-*`) and warm commerce palette (`commerce-cream`, `commerce-gold`, `commerce-terracotta`, etc.); content scans `templates/**` and `app/**`; safelist keeps component utilities in production build.
- Makefile targets: `make css` (minified build), `make css-watch`, `make install-tailwind`; `scripts/install-tailwind.sh` downloads Tailwind v3.4.17 standalone CLI (no Node.js) per OS/arch into `bin/tailwindcss`.
- `templates/base.html` body gets `min-h-screen bg-commerce-cream text-commerce-ink` for content scanning; `bin/tailwindcss` and `static/css/app.css` gitignored (generated).
- Verified: `make css` → `static/css/app.css` (~5KB) contains `.min-h-screen`, `.bg-commerce-cream`, `.bg-kapruka-600`, `.btn-primary`.
Files: static/css/input.css, tailwind.config.js, Makefile, scripts/install-tailwind.sh, templates/base.html, .gitignore
Next: PRD-007 Static assets mount (FastAPI StaticFiles at /static).

## PRD-007 — Static assets mount
- Mounted `StaticFiles` at `/static` in `create_app()` using `STATIC_DIR` resolved from project root (`Path(__file__).parent.parent / "static"`).
- Added `<link rel="stylesheet" href="/static/css/app.css" />` to `templates/base.html` head.
- `tests/conftest.py` session fixture ensures `static/css/app.css` exists in CI (file is gitignored, generated by `make css`).
- Unit tests: `test_static_css_returns_200_with_text_css`; templating test asserts CSS link in base.html.
Files: app/main.py, templates/base.html, tests/conftest.py, tests/unit/test_main.py, tests/unit/test_templating.py
Next: PRD-008 Async Redis client wrapper (lib/redis/client.py).

## PRD-008 — Async Redis client wrapper
- Added `lib/redis/client.py` with `RedisClient`: `connect()` via `redis.asyncio.from_url` (pool max_connections=10, socket_keepalive, health_check_interval, retry_on_timeout), `ping()` with one reconnect attempt on connection/timeout errors, idempotent `close()`, and `client` property for downstream cache/rate-limit modules.
- Dev dep `fakeredis[lua]` for in-memory integration tests without Docker Redis.
- Unit tests: fakeredis ping success, connect/close idempotency, connect() pool kwargs via monkeypatched from_url.
- Lifespan wiring deferred to PRD-011 (`get_redis` dependency); client wrapper is standalone.
Files: lib/redis/__init__.py, lib/redis/client.py, tests/unit/test_redis_client.py, pyproject.toml
Next: PRD-009 Sliding-window rate limiter (lib/redis/rate_limit.py).

## PRD-009 — Sliding-window rate limiter per client IP
- Added `lib/redis/rate_limit.py`: atomic sliding-window via Redis sorted sets + Lua script; 60 req/min global (`rate:{ip}:global`) and 30 `kapruka_create_order`/hour (`rate:{ip}:create_order`).
- `check_rate_limit(redis_client, ip, tool_name)` raises `RateLimitExceeded` with `retry_after_seconds`; `retry_after_header()` maps to HTTP `Retry-After`.
- All MCP tools share the global bucket; create_order also increments its dedicated hourly bucket.
- Unit tests: 60th allowed, 61st raises + httpx ASGI 429 with Retry-After, separate keys, create_order 31st block, shared global across read tools.
Files: lib/redis/rate_limit.py, lib/redis/__init__.py, tests/unit/test_rate_limit.py
Next: PRD-010 Read-only MCP response cache (lib/redis/cache.py).

## PRD-010 — Read-only MCP response cache
- Added `lib/redis/cache.py`: `get_cached` / `set_cached` with 30-min default TTL (`DEFAULT_CACHE_TTL=1800`); Redis key `tool:sha256(canonical_json)` via `canonical_args_json` (sorted keys, compact separators).
- `is_cacheable_tool` blocks `kapruka_create_order` (imported from rate_limit); read tools cache normally.
- Unit tests: cache miss→set→hit for `kapruka_search_products`, reordered-args equivalence, create_order bypass (no Redis keys written), key format and canonical JSON helpers.
Files: lib/redis/cache.py, lib/redis/__init__.py, tests/unit/test_cache.py
Next: PRD-011 FastAPI dependency injection for Redis (app/dependencies.py).

## PRD-011 — FastAPI dependency injection for Redis
- Lifespan connects `RedisClient` via `get_settings().redis_url` and stores on `app.state.redis`; shutdown closes via existing `_close_client`.
- Added `app/dependencies.py` with async `get_redis(request)` generator dependency; returns 503 when Redis unavailable.
- Unit tests: route DI resolves with fakeredis-injected client, 503 when state.redis is None, lifespan assigns connected client to app.state.
Files: app/dependencies.py, app/lifespan.py, tests/unit/test_dependencies.py
Next: PRD-012 Neo4j async driver wrapper (lib/neo4j/client.py).

## PRD-012 — Neo4j async driver wrapper
- Added `lib/neo4j/client.py` with `Neo4jClient`: `connect(uri, user, password)` via `AsyncGraphDatabase.driver` + `verify_connectivity()`, async `execute(cypher, params)` returning record dicts via `result.data()`, `health_check()` running `RETURN 1 AS ok`, idempotent `close()`.
- Pool config: connection_timeout=30s, max_connection_lifetime=3600s; mirrors RedisClient injectable-driver pattern for tests.
- Unit tests with mock AsyncDriver/session (no Docker testcontainers): health_check, execute param binding, connect verify + close idempotency.
Files: lib/neo4j/__init__.py, lib/neo4j/client.py, tests/unit/test_neo4j_client.py
Next: PRD-013 Zep Cloud client wrapper (lib/zep/client.py).

## PRD-013 — Zep Cloud client wrapper
- Added `lib/zep/client.py` with `ZepClient`: wraps `zep_python.client.AsyncZep`, explicit `base_url=https://api.getzep.com` (SDK default is broken when base_url omitted), `list_sessions`, `create_session` (maps to memory.add_session), `health_check` via list_sessions(page_size=1), idempotent `close()` on underlying httpx client.
- Injectable `httpx.AsyncClient` for tests; mirrors Neo4jClient/RedisClient connect + close pattern; lifespan wiring deferred (same as Neo4j PRD-012).
- Unit tests with httpx MockTransport: API key Authorization header, health_check success/failure, list_sessions, create_session, close idempotency.
Files: lib/zep/__init__.py, lib/zep/client.py, tests/unit/test_zep_client.py
Next: PRD-014 MCP HTTP JSON-RPC client (lib/kapruka/mcp_client.py).

## PRD-014 — MCP HTTP JSON-RPC client
- Added `lib/kapruka/mcp_client.py` with `MCPHttpClient`: uses MCP SDK `streamable_http_client` + `ClientSession` over HTTP JSON-RPC to `KAPRUKA_MCP_URL` (default `https://mcp.kapruka.com/mcp`).
- `call_tool(name, params)` wraps Kapruka's `{"params": ...}` argument shape, returns concatenated text content; per-call MCP session avoids anyio task-group lifecycle issues on reconnect.
- Retry loop: max 3 attempts, exponential backoff (0.5s, 1s) on 5xx `HTTPStatusError`, `TimeoutException`, and `ExceptionGroup` wrappers from the transport.
- Injectable `httpx.AsyncClient` for tests; `connect()` / `close()` mirror Redis/Neo4j/Zep client pattern.
- Unit tests: JSON-RPC `tools/call` envelope verification, 503 retry with backoff timing, max-retry exhaustion, no retry on 4xx, idempotent close.
Files: lib/kapruka/__init__.py, lib/kapruka/mcp_client.py, tests/unit/test_mcp_client.py
Next: PRD-015 Kapruka error hierarchy (lib/kapruka/errors.py).

## PRD-015 — Kapruka error hierarchy
- Added `lib/kapruka/errors.py`: `KaprukaError` base with `code`/`message`; `KaprukaRateLimitError` (429 + `retry_after_seconds` parsed from message or default 60s); `KaprukaValidationError` for checkout/input codes (`empty_cart`, `city_not_deliverable`, etc.); `KaprukaNotFoundError` for `product_not_found`/`order_not_found`.
- `parse_mcp_error(result_str)` matches live MCP `Error (<code>): <message>` payloads; also handles `Error: …` and MCP SDK `Error executing tool … validation error` strings; successful JSON passes through without raising.
- Verified live MCP formats: `Error (product_not_found): …`, `Error (order_not_found): …`.
- Unit tests: 429 with/without retry hint, empty_cart, product_not_found, order_not_found, success JSON no-op.
Files: lib/kapruka/errors.py, lib/kapruka/__init__.py, tests/unit/test_kapruka_errors.py
Next: PRD-016 Shared Pydantic types for MCP tools (lib/kapruka/types.py).

## PRD-016 — Shared Pydantic types for MCP tools
- Added `lib/kapruka/types.py` with Pydantic v2 input/output models for all 7 Kapruka MCP tools: search_products, get_product, list_categories, list_delivery_cities, check_delivery, create_order, track_order.
- Checkout models: `CartItem` (qty 1–99, icing_text max 120), `Recipient`, `Delivery` (YYYY-MM-DD date, location_type enum), `Sender`, `CreateOrderInput` (cart 1–30, gift_message max 300), `CreateOrderResponse` (order_ref, checkout_url, expires_at, summary).
- Shared helpers: `Money`, `CategoryRef`, currency/sort validators aligned with live MCP constraints.
- Unit tests: gift_message 300-char boundary + 301 rejection; icing_text 120-char boundary + 121 rejection; cart size bounds.
Files: lib/kapruka/types.py, lib/kapruka/__init__.py, tests/unit/test_kapruka_types.py
Next: PRD-017 Wrapper for kapruka_search_products (lib/kapruka/tools/search_products.py).

## PRD-017 — Wrapper for kapruka_search_products
- Added `lib/kapruka/tools/search_products.py`: async `search_products(client, *, q, category, min_price, max_price, in_stock_only, sort, limit, cursor, currency)` builds validated `SearchProductsInput`, forces `response_format=json`, calls MCP `kapruka_search_products`, parses via `parse_mcp_error` + `SearchProductsOutput.model_validate`.
- Handles plain-text `No products found for …` MCP responses as `KaprukaError(no_products_found)` before JSON parse.
- Unit tests: mocked JSON → typed `ProductResult` list, forced response_format, no-products error, input validation short-circuit.
Files: lib/kapruka/tools/{__init__,search_products}.py, lib/kapruka/__init__.py, tests/unit/test_search_products.py
Next: PRD-018 Wrapper for kapruka_get_product and kapruka_list_categories.

## PRD-018 — Wrapper for kapruka_get_product and kapruka_list_categories
- Added `lib/kapruka/tools/get_product.py`: async `get_product(client, *, product_id, currency, type)` builds validated `GetProductInput`, forces `response_format=json`, calls MCP `kapruka_get_product`, parses via `parse_mcp_error` + `GetProductOutput.model_validate`.
- Added `lib/kapruka/tools/list_categories.py`: async `list_categories(client, *, depth)` builds `ListCategoriesInput`, forces JSON, returns nested `ListCategoriesOutput` with `CategoryNode` children.
- Unit tests: fixture JSON for product detail (variants, shipping, category path) and nested category tree (depth=2); forced response_format; input validation short-circuit; product_not_found → KaprukaNotFoundError.
Files: lib/kapruka/tools/{get_product,list_categories}.py, lib/kapruka/{__init__,tools/__init__}.py, tests/unit/{test_get_product,test_list_categories}.py, prd.json
Next: PRD-019 Wrapper for delivery city and check_delivery tools (lib/kapruka/tools/delivery.py).

## PRD-019 — Wrapper for delivery city and check_delivery tools
- Added `lib/kapruka/tools/delivery.py`: async `list_delivery_cities(client, *, query, limit)` returns `list[str]` of canonical city names from `ListDeliveryCitiesOutput`; async `check_delivery(client, *, city, delivery_date, product_id)` validates via `CheckDeliveryInput` (YYYY-MM-DD) and returns typed `CheckDeliveryOutput` with `available` bool plus rate/reason/next_available_date.
- Both wrappers force `response_format=json`, call MCP via `parse_mcp_error` + Pydantic validate; `city_not_deliverable` and `date_not_deliverable` map to `KaprukaValidationError`.
- Unit tests: city name extraction, forced JSON format, limit/date validation short-circuit, available/unavailable payloads, both deliverability error codes.
Files: lib/kapruka/tools/delivery.py, lib/kapruka/tools/__init__.py, lib/kapruka/__init__.py, tests/unit/test_delivery.py, prd.json
Next: PRD-020 Wrapper for kapruka_create_order (lib/kapruka/tools/create_order.py).

## PRD-020 — Wrapper for kapruka_create_order
- Added `lib/kapruka/tools/create_order.py`: async `create_order(client, *, cart, recipient, delivery, sender, gift_message, currency)` builds validated `CreateOrderInput`, forces `response_format=json`, asserts `is_cacheable_tool` is False (write path never read-cached), calls MCP `kapruka_create_order`, parses via `parse_mcp_error` + `CreateOrderResponse.model_validate`.
- Pre-MCP validation: `_validate_cart_icing_text` rejects icing_text on non-cake SKUs (CAKE* prefix); cart/qty/gift_message bounds enforced by Pydantic models from PRD-016.
- Unit tests: full response field parsing (order_ref, checkout_url, expires_at, summary totals), forced JSON format, cache bypass, cart/qty/gift_message/icing_text validation short-circuit, empty_cart → KaprukaValidationError.
Files: lib/kapruka/tools/create_order.py, lib/kapruka/tools/__init__.py, lib/kapruka/__init__.py, tests/unit/test_create_order.py, prd.json
Next: PRD-021 Wrapper for kapruka_track_order (lib/kapruka/tools/track_order.py).

## PRD-021 — Wrapper for kapruka_track_order
- Added `lib/kapruka/tools/track_order.py`: async `track_order(client, *, order_number)` builds validated `TrackOrderInput`, forces `response_format=json`, calls MCP `kapruka_track_order`, parses via `parse_mcp_error` + `TrackOrderOutput.model_validate`.
- Reused PRD-016 `TrackOrderOutput` (status, status_display, delivery_date ETA, progress events, recipient, items) rather than introducing a duplicate TrackOrderResponse alias.
- Unit tests: full response field parsing (status, progress timeline, delivery_date), forced response_format, order_number min-length validation, order_not_found → KaprukaNotFoundError.
Files: lib/kapruka/tools/track_order.py, lib/kapruka/tools/__init__.py, lib/kapruka/__init__.py, tests/unit/test_track_order.py, prd.json
Next: PRD-022 KaprukaService facade with cache and rate limiting (lib/kapruka/service.py).

## PRD-022 — KaprukaService facade with cache and rate limiting
- Added `lib/kapruka/service.py` with `KaprukaService(redis_client, mcp_client)`: methods for all 7 MCP tools, each accepting `client_ip` for per-IP sliding-window limits via `check_rate_limit`.
- Read tools use `_cached_read`: cache lookup before MCP, `set_cached` (30-min TTL) on miss; `create_order` rate-limited only, never cached (delegates to PRD-020 wrapper).
- Integration tests (`tests/integration/test_kapruka_service.py`): second identical `search_products` skips MCP (TTL ≤ 1800s); repeated `create_order` always hits MCP with no Redis cache keys.
Files: lib/kapruka/service.py, lib/kapruka/__init__.py, tests/integration/test_kapruka_service.py, prd.json
Next: PRD-023 Chat viewport Jinja template with empty state (templates/chat/index.html).

## PRD-023 — Chat viewport Jinja template with empty state
- Created `templates/chat/index.html` extending `base.html`: full-viewport flex column, header with Kapruka branding, `#chat-messages` scroll container, `#chat-empty-state` welcome card with four gift-discovery suggested prompts.
- Warm commerce Tailwind styling: `card-commerce`, `kapruka-600` accent, `commerce-cream`/`commerce-parchment` palette; no HTMX form yet (PRD-024).
- Updated `app/routes/chat.py` to render `TemplateResponse` for GET `/chat` instead of JSON stub.
- Unit tests: `tests/unit/test_chat.py` (template isolation + GET /chat 200 HTML with empty state); removed obsolete JSON stub test from `test_main.py`.
Files: templates/chat/index.html, app/routes/chat.py, tests/unit/test_chat.py, tests/unit/test_main.py, prd.json
Next: PRD-024 HTMX chat input form (hx-post /chat/stream).

## PRD-024 — HTMX chat input form
- Added sticky footer chat form to `templates/chat/index.html`: textarea `name=message`, submit button, `hx-post="/chat/stream"`, `hx-ext="sse"`, `hx-target="#chat-messages"`, `hx-swap="beforeend"`, `hx-trigger="submit"`, `hx-indicator="#chat-loading"` with spinner.
- Created `templates/chat/message_user.html` user bubble partial; POST `/chat/stream` stub returns user HTML + OOB delete of `#chat-empty-state` (full SSE streaming deferred to PRD-037).
- Unit tests: form HTMX attributes in GET /chat HTML; POST returns swappable bubble; whitespace-only message rejected with 422.
Files: templates/chat/{index,message_user}.html, app/routes/chat.py, tests/unit/test_chat.py, prd.json
Next: PRD-025 Alpine.js focus and scroll helpers.

## PRD-025 — Alpine.js focus and scroll helpers
- Added `static/js/chat-helpers.js`: Alpine `chatHelpers` component with `scrollToBottom()` (sets `#chat-messages` scrollTop) and `focusInput()` (refocuses `#chat-message` textarea).
- Wired `htmx:afterSwap` on `#chat-messages` swaps to scroll; `htmx:afterRequest` on successful `#chat-form` submit to refocus input.
- Updated `templates/chat/index.html`: `x-data="chatHelpers()"` on viewport wrapper, `x-ref="messages"` / `x-ref="input"`, `min-h-0` flex fixes for scrollable message list; script loaded in `head_extra` before deferred Alpine.
- Unit tests: DOM attribute assertions in `test_chat.py`, JS source checks in `test_chat_helpers_js.py`; Playwright browser harness verifies scroll-to-bottom and input focus on HTMX events.
Files: static/js/chat-helpers.js, templates/chat/index.html, tests/unit/{test_chat,test_chat_helpers_js,test_chat_helpers_browser}.py, prd.json
Next: PRD-026 AgentState TypedDict schema (graphs/state.py).

## PRD-026 — AgentState TypedDict schema
- Added `graphs/state.py` with `AgentState` TypedDict: `messages` (Annotated reducer via `operator.add` on `list[BaseMessage]`), optional orchestration fields for intent, hybrid_context, tool_calls/results, tool_call_count, model_tier, session_id, zep_thread_id, currency, checkout_state, response_html.
- Literal aliases: `Intent`, `ModelTier`, `CheckoutStep`, `CurrencyCode` aligned with downstream PRD-028/032/067 routing and MCP currency codes.
- Unit tests: minimal dict type-check, all 12 field keys + messages reducer metadata, fully populated state assignment.
Files: graphs/{state,__init__}.py, tests/unit/test_agent_state.py, prd.json
Next: PRD-027 Redis LangGraph checkpointer (lib/redis/checkpointer.py).

## PRD-027 — Redis LangGraph checkpointer
- Added `lib/redis/checkpointer.py`: `create_checkpointer(redis_client)` binds `AsyncRedisSaver` to the shared pool; `get_checkpointer` calls `asetup()` for RediSearch indices (Memorystore / Redis Stack).
- Added `langgraph-checkpoint-redis>=0.3,<0.4` to pyproject.toml (compatible with langgraph 0.6.x / checkpoint 3.x).
- Added `graphs/shopping_graph.py` stub: `build_shopping_graph(checkpointer=…)` + `get_shopping_graph(redis_client)` factory; `bootstrap` placeholder node until PRD-028; helpers `initial_shopping_state` / `append_message_state`.
- Integration tests (`tests/integration/test_redis_checkpointer.py`): fakeredis + patched `asetup` (no RediSearch in fakeredis); asserts `tool_call_count` and `messages` persist across two `ainvoke` calls with same `thread_id`, and thread isolation.
Files: lib/redis/{checkpointer,__init__}.py, graphs/{shopping_graph,__init__}.py, pyproject.toml, tests/integration/test_redis_checkpointer.py, prd.json
Next: PRD-028 analyze_intent LangGraph node (graphs/nodes/analyze_intent.py).

## PRD-028 — analyze_intent LangGraph node
- Added `graphs/nodes/analyze_intent.py`: async `analyze_intent(state)` extracts latest `HumanMessage`, calls Gemini 2.5 Flash via google-genai with `response_mime_type=application/json` and Pydantic `IntentClassification` schema; sync SDK wrapped in `asyncio.to_thread`.
- Injectable `genai_client` for tests; `create_genai_client()` reads `GOOGLE_API_KEY` from Settings; empty/whitespace messages short-circuit to `general` without LLM call.
- Unit tests: discovery for "birthday cake for mom" (mocked genai), empty/no-message general default, JSON text fallback when `parsed` absent.
- Shopping graph still uses bootstrap stub until PRD-033 wires all nodes.
Files: graphs/nodes/{__init__,analyze_intent}.py, tests/unit/test_analyze_intent.py, prd.json
Next: PRD-029 retrieve_hybrid_context stub node (graphs/nodes/retrieve_hybrid_context.py).

## PRD-029 — retrieve_hybrid_context stub node
- Added `graphs/nodes/retrieve_hybrid_context.py`: async `retrieve_hybrid_context(state)` returns `{"hybrid_context": {}}` stub (Neo4j deferred to PRD-047); `route_after_analyze_intent(state)` routes tracking/checkout directly to `call_mcp_tools`, discovery/general to `retrieve_hybrid_context`.
- Unit tests: empty dict return without Neo4j imports, routing matrix for all intents, missing intent defaults to retrieve.
- Graph wiring deferred to PRD-033; routing helper ready for conditional edges.
Files: graphs/nodes/{retrieve_hybrid_context,__init__}.py, tests/unit/test_retrieve_hybrid_context.py, prd.json
Next: PRD-030 call_mcp_tools LangGraph node (graphs/nodes/call_mcp_tools.py).

## PRD-030 — call_mcp_tools LangGraph node
- Added `graphs/nodes/call_mcp_tools.py`: `select_tool_calls(state)` picks explicit `tool_calls` when present, else intent defaults — discovery → search_products (or get_product when Kapruka product id detected in message), general → list_categories; tracking/checkout deferred to later PRDs.
- `call_mcp_tools(state, kapruka_service=…, client_ip=…)` invokes KaprukaService, merges serialized Pydantic outputs into `tool_results`, increments `tool_call_count` per invocation; requires injected service (graph wiring in PRD-033).
- Unit tests: discovery search mock, general list_categories, explicit tool_calls override, count increment, product-id routing helper.
Files: graphs/nodes/{call_mcp_tools,__init__}.py, tests/unit/test_call_mcp_tools.py, prd.json
Next: PRD-031 generate_response LangGraph node (graphs/nodes/generate_response.py).

## PRD-031 — generate_response LangGraph node
- Added `graphs/nodes/generate_response.py`: async `generate_response(state)` calls Gemini (flash default, pro when `model_tier=pro`) with faithfulness system prompt + serialized `tool_results` JSON; structured `AssistantReply` schema; renders `templates/chat/message_assistant.html` into `response_html`.
- Created `templates/chat/message_assistant.html` assistant bubble partial (left-aligned, commerce styling); product carousel slot deferred to PRD-039/055.
- Injectable `genai_client`; empty user message short-circuits without LLM; `render_assistant_html()` exposed for tests.
- Unit tests: product names from mocked tool_results appear in response_html, empty message skip, JSON text fallback parsing, template structure.
Files: graphs/nodes/{generate_response,__init__}.py, templates/chat/message_assistant.html, tests/unit/test_generate_response.py, prd.json
Next: PRD-032 Model router Flash to Pro escalation (graphs/model_router.py).

## PRD-032 — Model router Flash to Pro escalation
- Added `graphs/model_router.py`: `select_model(state)` returns `gemini-2.5-flash` or `gemini-2.5-pro`; `select_model_tier(state)` mirrors tier logic for PRD-069 checkout review wiring.
- Escalation rules: explicit `model_tier=pro`, `checkout_state=review`, or `tool_call_count > 3` (4th tool call onward) → Pro; otherwise Flash.
- Wired `generate_response` to use central `select_model()` instead of local stub; exported from `graphs/__init__.py`.
- Unit tests: default flash, review escalation, threshold boundary (3=flash, 4=pro), explicit pro tier.
Files: graphs/{model_router,__init__}.py, graphs/nodes/generate_response.py, tests/unit/{test_model_router,test_generate_response}.py, prd.json
Next: PRD-033 Compile main shopping StateGraph (graphs/shopping_graph.py).

## PRD-033 — Compile main shopping StateGraph
- Replaced bootstrap stub in `graphs/shopping_graph.py` with full StateGraph: START → analyze_intent → conditional (`route_after_analyze_intent`) → retrieve_hybrid_context | call_mcp_tools → call_mcp_tools → generate_response → END.
- Added `ShoppingGraphDeps` dataclass for injectable `kapruka_service`, `client_ip`, `genai_client`; node wrappers bind deps at compile time for testability and PRD-037 chat route wiring.
- `build_shopping_graph(checkpointer=…, deps=…)` and `get_shopping_graph(redis_client, deps=…)` export compiled graph with Redis checkpointer.
- Integration tests (`tests/integration/test_shopping_graph.py`): end-to-end discovery flow, tracking skips hybrid context, astream_events node order assertion.
- Updated `tests/integration/test_redis_checkpointer.py` to use full graph + mocks (tool_call_count persistence still verified).
Files: graphs/{shopping_graph,__init__}.py, tests/integration/{test_shopping_graph,test_redis_checkpointer}.py, prd.json
Next: PRD-034 Zep session create and resume (lib/zep/session.py).

## PRD-034 — Zep session create and resume
- Added `lib/zep/session.py`: `get_or_create_session(redis_client, zep_client, session_id)` returns `zep_thread_id`; Redis key `zep:session:{session_id}` with 7-day TTL; resume refreshes TTL via EXPIRE; first visit calls `ZepClient.create_session`.
- Used browser `session_id` as Zep thread id (1:1 mapping) — Redis TTL enables re-init after expiry; PRD-035 can wire into chat route.
- Unit tests (`tests/unit/test_zep_session.py`): fakeredis + mocked Zep HTTP; asserts create on first visit, resume without second POST, TTL set.
Files: lib/zep/{session,__init__}.py, tests/unit/test_zep_session.py, prd.json
Next: PRD-035 Zep memory read and write in graph loop.

## PRD-035 — Zep memory read and write in graph loop
- Added `lib/zep/memory.py`: `get_session_memory_facts`, `append_session_messages`, `extract_memory_facts`, `format_memory_facts_block` (last 10 facts default).
- Extended `ZepClient` with `get_memory()` and `add_messages()` wrapping SDK memory.get/add.
- New graph nodes: `load_zep_memory` (START → before analyze_intent) loads facts into `zep_memory_facts`; `zep_memory_write` (after generate_response → END) persists user/assistant turn.
- `AgentState` gains `zep_memory_facts` and `assistant_message`; `analyze_intent` and `generate_response` inject facts into system prompts.
- `ShoppingGraphDeps` accepts optional `zep_client`; graph skips memory I/O when absent (tests without Zep).
- Integration tests (`tests/integration/test_zep_memory_graph.py`): mocked Zep HTTP verifies GET facts loaded and POST messages persisted after full graph turn.
Files: lib/zep/{memory,client,__init__}.py, graphs/{state,shopping_graph}.py, graphs/nodes/{load_zep_memory,zep_memory_write,analyze_intent,generate_response,__init__}.py, tests/unit/test_zep_memory.py, tests/integration/test_zep_memory_graph.py, tests/integration/test_shopping_graph.py, tests/unit/test_agent_state.py, prd.json
Next: PRD-036 Cross-session preference extraction.

## PRD-036 — Cross-session preference extraction
- Added `lib/zep/preferences.py`: `extract_preferences(zep_client, zep_thread_id)` uses Zep `search_session_memory` (facts scope) with get_memory fallback; `parse_preferences_from_facts` extracts favorite_category, past_occasion, currency; `merge_preferences_into_hybrid_context` builds MCP hints.
- Extended `ZepClient` with `search_session_memory` wrapping SDK `search_sessions`.
- Updated `retrieve_hybrid_context` to call extract_preferences (or parse loaded zep_memory_facts) and inject currency into AgentState when unset.
- Updated `call_mcp_tools.select_tool_calls` to apply category/currency hints from hybrid_context on discovery search.
- Wired `zep_client` into retrieve_hybrid_context node in shopping graph.
- Unit tests: `test_zep_preferences.py`, category preference in `test_call_mcp_tools.py`, hybrid context merge in `test_retrieve_hybrid_context.py`.
Files: lib/zep/{preferences,client,__init__}.py, graphs/nodes/{retrieve_hybrid_context,call_mcp_tools}.py, graphs/shopping_graph.py, tests/unit/{test_zep_preferences,test_retrieve_hybrid_context,test_call_mcp_tools}.py, prd.json
Next: PRD-037 SSE streaming chat endpoint.

## PRD-037 — SSE streaming chat endpoint
- Replaced POST `/chat/stream` HTML stub with `StreamingResponse` (`text/event-stream`) driving `graph.astream(stream_mode="updates")`.
- Added `lib/chat/`: `sse.format_sse_event` + `chunk_text`, `streaming.iter_chat_sse_events` (user bubble → progressive assistant chunks via OOB → final `response_html`), `deps` for lazy KaprukaService/MCP + graph compile, `session` cookie (`ak_session`).
- Route wires Redis checkpointer thread_id=session_id, Zep `get_or_create_session` when zep client present, `resolve_turn_state` for checkpoint follow-ups.
- Unit tests: `test_chat_sse.py` (SSE framing + stream iterator), updated `test_chat.py` httpx async SSE integration with mocked graph.
Files: app/routes/chat.py, lib/chat/{__init__,sse,streaming,deps,session}.py, tests/unit/{test_chat,test_chat_sse}.py, prd.json
Next: PRD-038 HTMX SSE extension wiring (sse-connect/sse-swap on chat form).

## PRD-038 — HTMX SSE extension wiring
- `base.html` already ships `hx-ext="sse"` + `htmx-ext-sse@2.2.2`; chat form now uses `sse-connect="/chat/stream"` and a hidden `#chat-sse-listener` with `sse-swap="message"` targeting `#chat-messages` (`beforeend`).
- Added `static/js/chat-sse.js`: bridges POST `/chat/stream` (SSE extension only supports GET EventSource) via fetch + `htmx.swap`, stubs `htmx.createEventSource` for the connect path, dispatches `htmx:afterSwap`/`htmx:afterRequest` for Alpine chatHelpers.
- Playwright harness (`test_chat_sse_browser.py`) verifies incremental `message` events append into `#chat-messages`; static contract test for `chat-sse.js`.
Files: templates/chat/index.html, static/js/chat-sse.js, tests/unit/{test_chat,test_chat_sse_js,test_chat_sse_browser}.py, prd.json
Next: PRD-039 Assistant message Jinja partial (products carousel slot).

## PRD-039 — Assistant message Jinja partial
- Enhanced `templates/chat/message_assistant.html`: `role="assistant"`, `data-role="assistant-message"`, `.prose-assistant` wrapper, optional `products_html` slot (`data-slot="product-carousel"`) for inline carousel injection via `| safe`.
- Added `.prose-assistant` component styles in `static/css/input.css` (lists, links, whitespace) without `@tailwindcss/typography`; safelisted chat bubble utilities in `tailwind.config.js`.
- Extended `render_assistant_html(message, *, products_html=None)` in `generate_response.py`; aligned streaming partial `role="assistant"` in `lib/chat/streaming.py`.
- Isolation tests: `tests/unit/test_message_assistant.py` (structure, products block, XSS escape, safe carousel HTML); updated `test_generate_response.py` assertions.
Files: templates/chat/message_assistant.html, static/css/input.css, tailwind.config.js, graphs/nodes/generate_response.py, lib/chat/streaming.py, tests/unit/{test_message_assistant,test_generate_response}.py, prd.json
Next: PRD-041 Neo4j ontology Cypher schema (PRD-040 already passes).

## PRD-040 — Chat route integration test
- Added `tests/integration/test_chat_flow.py`: httpx `AsyncClient` POSTs `show me birthday cakes` to `/chat/stream`, collects SSE body, asserts `role="assistant"`, `data-role="assistant-message"`, user message present, no error alert markup.
- Mocks `build_shopping_graph_deps` with `ShoppingGraphDeps` (mocked `KaprukaService` + Gemini); real `get_compiled_chat_graph` + full shopping StateGraph run.
- Patches `AsyncRedisSaver.asetup` for fakeredis (same FT._LIST workaround as `test_redis_checkpointer.py`).
Files: tests/integration/test_chat_flow.py, prd.json
Next: PRD-041 Neo4j ontology Cypher schema.

## PRD-041 — Neo4j ontology Cypher schema
- Added `lib/neo4j/ontology.py`: labels (Occasion, Category, ProductType), relationships (OCCASION_TO_CATEGORY, CATEGORY_TO_PRODUCT_TYPE), unique `id` constraints per label via `CREATE CONSTRAINT … IF NOT EXISTS`, Pydantic property schemas (`OntologyNodeProperties`, relationship weight models).
- `apply_ontology_schema` / `verify_ontology_schema` / `list_ontology_constraints` for migration + SHOW CONSTRAINTS verification.
- Added `scripts/migrate_ontology.py` CLI applying schema against Settings Neo4j URI and printing verified constraints.
- Unit tests (`tests/unit/test_neo4j_ontology.py`): recording mock driver verifies all constraint Cypher runs and `verify_ontology_schema` passes post-migration.
- Exported ontology helpers from `lib/neo4j/__init__.py` for PRD-042 ingest script.
Files: lib/neo4j/{ontology,__init__}.py, scripts/migrate_ontology.py, tests/unit/test_neo4j_ontology.py, prd.json
Next: PRD-042 Category ingestion script (scripts/ingest_categories.py).

## PRD-042 — Category ingestion script
- Added `lib/neo4j/ingest_categories.py`: maps Kapruka tree L0→Category, L1→Occasion, L2→ProductType (synthesizes ProductType from L0+L1 when grandchildren absent); batched UNWIND MERGE for nodes and OCCASION_TO_CATEGORY / CATEGORY_TO_PRODUCT_TYPE edges; `count_ontology_nodes_by_label` for verification.
- `INGEST_CATEGORY_DEPTH=2` — MCP `kapruka_list_categories` caps depth at 2 (PRD step cites 3; triplets complete via synthesized ProductType ids on two-level branches).
- Added `scripts/ingest_categories.py` CLI: KaprukaService fetch → apply ontology schema → ingest → print label counts.
- Unit tests (`tests/unit/test_ingest_categories.py`): taxonomy mapping, batched MERGE, full ingest + COUNT BY LABEL on recording mock driver.
Files: lib/neo4j/{ingest_categories,__init__}.py, scripts/ingest_categories.py, tests/unit/test_ingest_categories.py, prd.json
Next: PRD-043 Ontology node property enrichment.

## PRD-043 — Ontology node property enrichment
- Extended `lib/neo4j/ingest_categories.py`: `collect_node_enrichments` derives slug, display_name, description, and kapruka_id (URL path segment) per Category/Occasion/ProductType; MERGE Cypher SETs properties on all node types during ingest.
- `kapruka_id_from_url` extracts trailing browse path segment; contextual descriptions built per label for embedding (PRD-045).
- Added `fetch_ontology_node_properties` for post-ingest verification queries.
- Unit tests: enrichment mapping, kapruka_id extraction, Birthday occasion slug/display_name/kapruka_id assertion via mock store + fetch.
Files: lib/neo4j/ingest_categories.py, tests/unit/test_ingest_categories.py, prd.json
Next: PRD-044 Vertex AI text-embedding-005 client.

## PRD-044 — Vertex AI text-embedding-005 client
- Added `lib/embeddings/vertex_embeddings.py`: `embed_texts(texts)` async wrapper around Vertex `TextEmbeddingModel` (`text-embedding-005`, 768 dims); `vertexai.init` uses `gcp_project_id` / `gcp_location` from Settings; blocking SDK call via `asyncio.to_thread`; optional `model` injection for tests.
- Exported `EMBEDDING_MODEL`, `EMBEDDING_DIMENSION`, `embed_texts` from `lib/embeddings/__init__.py`.
- Unit tests (`tests/unit/test_vertex_embeddings.py`): mocked model asserts 768-dim vector, batch support, empty-list short-circuit, Settings-driven `vertexai.init` + `from_pretrained`.
Files: lib/embeddings/{__init__,vertex_embeddings}.py, tests/unit/test_vertex_embeddings.py, prd.json
Next: PRD-045 Batch embed ontology nodes.

## PRD-045 — Batch embed ontology nodes
- Added `lib/neo4j/embed_ontology.py`: `fetch_nodes_missing_embedding`, `build_embedding_text` (display_name + description), batched `set_node_embeddings` via UNWIND, `embed_ontology_nodes` orchestrating Vertex `embed_texts` (injectable for tests); `has_category_embeddings` + `count_nodes_with_embedding` for verification.
- Added `scripts/embed_ontology.py` CLI: connect Neo4j → embed missing nodes → assert Category embedding present → print per-label counts.
- Unit tests (`tests/unit/test_embed_ontology.py`): mock store + fake embed_fn covers fetch, batch SET, full embed run, skip when already embedded, PRD verify query, vector count mismatch.
Files: lib/neo4j/{embed_ontology,__init__}.py, scripts/embed_ontology.py, tests/unit/test_embed_ontology.py, prd.json
Next: PRD-046 Neo4j vector index creation and query.

## PRD-046 — Neo4j vector index creation and query
- Added `lib/neo4j/vector_search.py`: `create_category_vector_index` (768-dim cosine index `ontology_category_embedding` on `Category.embedding`), `vector_search(client, query_embedding, top_k=5)` via `db.index.vector.queryNodes`, `VectorSearchHit` dataclass, `has_category_vector_index` verification helper.
- Exported vector search API from `lib/neo4j/__init__.py`.
- Unit/integration tests (`tests/unit/test_vector_search.py`): mock store with cosine similarity simulates `queryNodes`; verifies index CREATE Cypher, id+score return shape, dimension/top_k validation, and that "birthday gift" ranks `category:cakes` above `category:flowers`.
Files: lib/neo4j/{vector_search,__init__}.py, tests/unit/test_vector_search.py, prd.json
Next: PRD-047 HybridRAG retrieve_hybrid_context full implementation.

## PRD-047 — HybridRAG retrieve_hybrid_context full implementation
- Replaced Zep-only stub in `graphs/nodes/retrieve_hybrid_context.py`: embed user message (Vertex `embed_texts`, injectable), `vector_search` top-5 categories, `traverse_from_categories` 2-hop ontology walk, merge graph hints then Zep preferences (Zep wins on conflicts); graceful fallback when Neo4j fails.
- Added `lib/neo4j/traverse.py` (`TraversalResult`, `traverse_from_categories`) and `lib/neo4j/hybrid_context.py` (`build_graph_hybrid_context`, `fetch_category_display_names`).
- Wired `neo4j_client` through `ShoppingGraphDeps` into retrieve node.
- Tests: `tests/integration/test_hybrid_rag.py` (wedding flowers → Flowers category + Wedding occasion hints), `tests/unit/test_traverse.py`, updated `test_retrieve_hybrid_context.py`.
Files: graphs/nodes/retrieve_hybrid_context.py, graphs/shopping_graph.py, lib/neo4j/{traverse,hybrid_context,__init__}.py, tests/{integration/test_hybrid_rag,unit/test_traverse,unit/test_retrieve_hybrid_context}.py, prd.json
Next: PRD-048 Neo4j multi-hop traversal helper.

## Review hardening — signed chat sessions (post PRD-037–044)
- `lib/chat/session.py`: HMAC-signed `ak_session` cookie bound to `SESSION_SECRET`; rejects client-forged thread IDs (fixes checkpoint IDOR/session fixation).
- `app/routes/chat.py`: yield SSE error fragment on pre-stream failures; `Secure` cookie when `APP_ENV=production`.
- `lib/chat/deps.py`: honor `X-Forwarded-For` only behind trusted proxy (production or localhost peer).
- Tests: `test_chat_session.py`, `test_chat_deps.py`, setup-failure + cookie reuse in `test_chat.py`.
Files: lib/chat/session.py, app/routes/chat.py, lib/chat/deps.py, tests/unit/{test_chat_session,test_chat_deps,test_chat}.py

## PRD-048 — Neo4j multi-hop traversal helper
- Core shipped in PRD-047 (`lib/neo4j/traverse.py`): `traverse_from_categories` runs parameterized variable-length Cypher along `OCCASION_TO_CATEGORY` / `CATEGORY_TO_PRODUCT_TYPE`, returns `TraversalResult` with hop depth, relationship type, and path weight product.
- Hardened unit tests (`tests/unit/test_traverse.py`): fixture graph now includes sibling category `category:cakes` reachable in 2 hops (flowers → wedding → cakes); asserts weights, seed_id, empty-seed short-circuit, invalid max_hops.
Files: lib/neo4j/traverse.py, tests/unit/test_traverse.py, lib/neo4j/__init__.py, prd.json
Next: PRD-049 Graph-informed MCP filter injection.

## PRD-049 — Graph-informed MCP filter injection
- Added `build_discovery_search_args` in `lib/neo4j/hybrid_context.py`: maps `hints.category` (top graph traversal category) to MCP `category` filter; augments `q` with `hints.occasion` when top `vector_hits[0].score` ≥ `VECTOR_CONFIDENCE_THRESHOLD` (0.65) and occasion terms absent from user message.
- Updated `graphs/nodes/call_mcp_tools.select_tool_calls` to use graph-informed search args for discovery intent instead of inline category/q assembly.
- Unit tests: birthday graph context → `category='Birthday'` + occasion-augmented `q`; low-confidence vector score skips occasion augmentation.
Files: lib/neo4j/hybrid_context.py, graphs/nodes/call_mcp_tools.py, tests/unit/test_call_mcp_tools.py, prd.json
Next: PRD-050 Product card Jinja partial.

## PRD-050 — Product card Jinja partial
- Added `templates/components/product_card.html`: Tailwind `card-commerce` with hover shadow/scale, image link, inline stock badge overlay (green In Stock / muted Out of Stock), `format_currency` price, and `hx-post /cart/add` button targeting `#cart-panel`.
- Supports product dict with scalar or Money-shaped `price`; optional `currency` when price is numeric.
- Added `render_product_card` + `normalize_html_snapshot` in `app/templating.py` for reuse and golden-file comparisons.
- Tests: `tests/unit/test_product_card.py` with JSON fixtures (`tests/fixtures/products/`) and HTML snapshot (`tests/fixtures/snapshots/product_card_in_stock.html`); out-of-stock overlay + disabled button coverage.
Files: templates/components/product_card.html, app/templating.py, tests/unit/test_product_card.py, tests/fixtures/{products,snapshots}/*
Next: PRD-051 Horizontal product carousel partial.

## PRD-051 — Horizontal product carousel partial
- Added `templates/components/product_carousel.html`: horizontal `flex overflow-x-auto` track with `snap-x snap-mandatory`, `overscroll-x-contain`, and `min-w-0` outer wrapper; embeds `product_card` partials in `snap-start shrink-0` wrappers.
- Alpine.js prev/next buttons (`scrollCarousel`) with arrow-key handlers on focusable region for keyboard navigation.
- Added `render_product_carousel` in `app/templating.py`; third fixture product for 3-card carousel tests.
- Tests: `tests/unit/test_product_carousel.py` (structure, Alpine nav, mobile containment classes); `tests/unit/test_product_carousel_browser.py` Playwright mobile viewport (375px) asserts no page horizontal overflow while track scrolls internally.
Files: templates/components/product_carousel.html, app/templating.py, tests/unit/test_product_carousel.py, tests/unit/test_product_carousel_browser.py, tests/fixtures/products/sample_vanilla_cake.json, prd.json
Next: PRD-052 Category filter chips partial.

## PRD-052 — Category filter chips partial
- Added `templates/components/category_chips.html`: HTMX filter chips from `hybrid_context.categories` with `hx-get /partials/search?category={name}`, `hx-target="#results"`, `hx-swap="innerHTML"`, `hx-trigger="click"`; `chip-active` / `chip-filter` Tailwind variants and `aria-pressed` for active state.
- Extended `app/templating.py`: `urlencode` Jinja filter, `categories_for_chips(hybrid_context)`, `render_category_chips()`.
- Added `GET /partials/search` stub in `app/routes/partials.py` returning HTML results fragment (live MCP search deferred to PRD-055).
- Chip component styles in `static/css/input.css`; safelisted in `tailwind.config.js`.
- Tests: `test_category_chips.py` (HTMX attrs, active variant, URL encoding, hybrid_context extraction, partials route); `test_category_chips_browser.py` Playwright harness via routed `http://localhost/` (HTMX 2 requires real document URL, not `about:blank` from `set_content`) — chip click swaps `#results` without navigation.
Files: templates/components/category_chips.html, app/templating.py, app/routes/partials.py, static/css/input.css, tailwind.config.js, tests/fixtures/hybrid_context/sample_categories.json, tests/unit/{test_category_chips,test_category_chips_browser}.py, prd.json
Next: PRD-053 Stock badge overlay component.

## PRD-053 — Stock badge overlay component
- Added `templates/components/stock_badge.html`: green "In Stock" badge (`data-stock-level`), amber "Low Stock" when `stock_level=low`, gray full-image overlay for out-of-stock with `data-stock-level` metadata.
- Refactored `product_card.html` to `{% include %}` stock_badge via `{% with in_stock, stock_level %}`; added `render_stock_badge()` in `app/templating.py`.
- Fixtures gained `stock_level`; snapshot updated with `data-stock-level="high"`.
- Tests: `tests/unit/test_stock_badge.py` (partial isolation + product_card integration for both stock states).
Files: templates/components/{stock_badge,product_card}.html, app/templating.py, tests/unit/test_stock_badge.py, tests/fixtures/products/{sample_in_stock,sample_out_of_stock}.json, tests/fixtures/snapshots/product_card_in_stock.html, prd.json
Next: PRD-054 Alpine lazy-load image animations.

## PRD-054 — Alpine lazy-load image animations
- Added `static/js/lazy-image.js`: Alpine `lazyImage` component with `IntersectionObserver` (64px rootMargin), pulse skeleton until load, `opacity-0` → `opacity-100` transition on `@load`.
- `product_card.html`: optional `lazy_image` flag wraps image in lazy loader (`x-if="inView"` defers `<img>` until scroll); standalone cards keep native `loading="lazy"`.
- `product_carousel.html` passes `lazy_image=true` per card; `chat/index.html` loads lazy-image.js globally for embedded carousels.
- Tests: `test_lazy_image_js.py`; carousel structure asserts lazy markup; Playwright scroll test stubs delayed PNGs and verifies off-screen cards defer load then fade in after `scrollLeft`.
Files: static/js/lazy-image.js, templates/components/{product_card,product_carousel}.html, templates/chat/index.html, tests/unit/{test_lazy_image_js,test_product_carousel,test_product_carousel_browser,test_chat}.py, prd.json
Next: PRD-055 Embed product carousel in assistant messages.

## PRD-055 — Embed product carousel in assistant messages
- Added `extract_search_products` and `build_products_carousel_html` in `graphs/nodes/generate_response.py`: reads `kapruka_search_products` from `tool_results`, renders `product_carousel` via `render_product_carousel`, passes `products_html` into `message_assistant.html` below assistant prose.
- `message_assistant.html` already had `products_html` slot from PRD-039; no template change required.
- Unit tests: carousel markup in `test_generate_response.py` (extract, build, empty-results skip); integration test `test_chat_flow.py` mocks search with product fixture and asserts `data-testid="product-carousel"` in SSE stream.
Files: graphs/nodes/generate_response.py, tests/unit/test_generate_response.py, tests/integration/test_chat_flow.py, prd.json
Next: PRD-056 Currency formatting Jinja filter.

## PRD-056 — Currency formatting Jinja filter
- Added `lib/utils/currency.py`: `format_currency(amount, currency)` for LKR, USD, GBP, AUD, CAD, EUR; LKR renders as `Rs. {n:,}` (no decimals, rounded); decimal currencies use symbol prefix with `,.2f`.
- `app/templating.py` imports `format_currency` from `lib.utils.currency` (Jinja filter registration unchanged).
- Tests: `tests/unit/test_currency.py` (PRD assertion + decimal/rounding/validation); updated product card snapshot and carousel tests to `Rs.` LKR display.
Files: lib/utils/{__init__,currency}.py, app/templating.py, tests/unit/{test_currency,test_templating,test_product_card,test_product_carousel}.py, tests/fixtures/snapshots/product_card_in_stock.html, prd.json
## PRD-057 — Header currency selector with HTMX
- Added `lib/redis/session.py`: `get_session_currency` / `set_session_currency` with Redis key `session:{id}:currency` (7-day TTL).
- Added `POST /session/currency` (`app/routes/session.py`): HTMX form post, validates against `SUPPORTED_CURRENCIES`, sets signed `ak_session` cookie on first visit.
- `templates/components/currency_selector.html` in `base.html` header (`hx-post`, `hx-swap="none"`, `hx-trigger="change"`); chat page uses `header_start` / `content` / `footer` blocks.
- `lib/chat/page_context.py` loads currency for GET `/chat` from Redis when session cookie present.
- Tests: `tests/unit/test_session_currency.py` (Redis get/set, HTMX attrs, POST persistence, refresh retains EUR selection).
Files: lib/redis/session.py, lib/chat/page_context.py, app/routes/{session,chat}.py, app/main.py, app/templating.py, templates/{base,chat/index}.html, templates/components/currency_selector.html, tests/unit/{test_session_currency,test_chat,test_templating}.py, prd.json
Next: PRD-058 Propagate session currency to all MCP calls.

## PRD-058 — Propagate session currency to all MCP calls
- `_chat_event_stream` loads `get_session_currency(redis, thread_id)` before graph invocation; `resolve_turn_state` seeds/refreshes `AgentState.currency` on every turn (first + follow-up via `initial_shopping_state` / `append_message_state`).
- `call_mcp_tools`: `_resolve_currency` prefers session state; `_inject_currency_args` ensures `search_products` and `get_product` always receive currency (explicit LLM tool_calls included).
- Tests: `test_chat_deps` currency seed/refresh; `test_call_mcp_tools_injects_session_currency_into_explicit_tool_calls`; integration `test_chat_stream_search_uses_session_currency_after_update` (POST /session/currency USD → POST /chat/stream → search_products currency=USD).
Files: app/routes/chat.py, lib/chat/deps.py, graphs/{shopping_graph,nodes/call_mcp_tools}.py, tests/unit/{test_chat_deps,test_call_mcp_tools}.py, tests/integration/test_chat_flow.py, prd.json
Next: PRD-059 Redis server-side cart state.

## PRD-059 — Redis server-side cart state
- Added `lib/redis/cart.py`: Redis hash `cart:{session_id}` stores JSON-serialized `StoredCartItem` rows (product_id, quantity, icing_text, name, price snapshot); 7-day TTL aligned with session prefs.
- API: `add_item` (merge qty for existing product_id), `remove_item`, `update_quantity`, `get_cart`, `clear_cart`; `CartLimitExceeded` on 31st distinct line; quantity bounded 1–99.
- Exported cart helpers from `lib/redis/__init__.py`.
- Tests: `tests/unit/test_redis_cart.py` (CRUD, merge, limit, quantity bounds, clear).
Files: lib/redis/{cart,__init__}.py, tests/unit/test_redis_cart.py, prd.json
Next: PRD-060 Cart HTMX partial swaps.

## PRD-060 — Cart HTMX partial swaps
- Added `templates/checkout/cart_partial.html`: `#cart-panel` root with line items, quantity stepper (+/− via `/cart/update`), remove buttons (`/cart/remove`), line totals via `format_currency`, empty state, `data-item-count` for badge wiring in PRD-061.
- Implemented `app/routes/cart.py`: `POST /cart/add` fetches product via KaprukaService + session currency, persists to Redis cart, returns partial; `/cart/remove` and `/cart/update` (qty&lt;1 removes) also return refreshed partial with session cookie on first visit.
- Added `render_cart_partial()` in `app/templating.py`.
- Tests: `tests/unit/test_cart_partial.py` (HTMX attrs, stepper bounds, empty state); `tests/integration/test_cart_flow.py` (add returns product name HTML, update/remove swap partial + Redis state).
Files: templates/checkout/cart_partial.html, app/routes/cart.py, app/templating.py, tests/unit/test_cart_partial.py, tests/integration/test_cart_flow.py, prd.json
Next: PRD-061 Alpine cart drawer.

## PRD-061 — Alpine cart drawer
- Added `static/js/cart-drawer.js`: Alpine `cartDrawer` with open/close, slide/fade transitions, badge `itemCount` synced from `#cart-panel[data-item-count]` on `htmx:afterSwap` (document.body listener, same pattern as chatHelpers).
- Added `templates/components/cart_drawer.html`: header cart icon + badge, fixed backdrop/panel slide-over embedding `cart_partial.html`; included from `base.html` beside currency selector with `cart-drawer.js` loaded before deferred Alpine.
- Extended `lib/chat/page_context.py`: `resolve_page_cart`, `cart_template_context`; chat GET passes cart lines for server-rendered badge and drawer content.
- Added `render_cart_drawer()` in `app/templating.py`.
- Tests: `test_cart_drawer.py` (Alpine markup, badge count, base.html wiring); `test_cart_drawer_browser.py` Playwright (open, backdrop close, Escape close, HTMX badge sync).
Files: static/js/cart-drawer.js, templates/{base,components/cart_drawer}.html, lib/chat/page_context.py, app/{routes/chat,templating}.py, tests/unit/{test_cart_drawer,test_cart_drawer_browser,test_chat,test_templating}.py, prd.json
Next: PRD-062 Delivery city debounced autocomplete.

## PRD-062 — Delivery city debounced autocomplete
- Added `templates/checkout/delivery_city.html`: city input with `hx-get /partials/delivery-cities`, `hx-trigger="keyup changed delay:300ms"`, swaps suggestions into `#delivery-city-suggestions`.
- Added `templates/checkout/delivery_city_suggestions.html`: `li` suggestion rows from Kapruka city names.
- Extended `app/routes/partials.py`: `GET /partials/delivery-cities` calls `KaprukaService.list_delivery_cities` (min 2-char query); `render_delivery_city` / `render_delivery_city_suggestions` in `app/templating.py`.
- Tests: `test_delivery_city.py` (HTMX debounce attrs, li partial); `test_delivery_city_flow.py` integration (`q=Col` → Colombo HTML, short query skips MCP).
Files: templates/checkout/{delivery_city,delivery_city_suggestions}.html, app/{routes/partials,templating}.py, tests/unit/test_delivery_city.py, tests/integration/test_delivery_city_flow.py, prd.json
Next: PRD-063 Colombo timezone delivery date picker.

## PRD-063 — Colombo timezone delivery date picker
- Added `lib/utils/timezone.py`: `colombo_today` / `colombo_today_iso` via `zoneinfo.ZoneInfo("Asia/Colombo")`; `is_past_colombo_date` for server-side past-date gate before MCP.
- Added `templates/checkout/delivery_date.html`: date input `min` from Colombo today, `hx-post /checkout/check-delivery` on change with city via `hx-include`; status target `#delivery-date-status`.
- Added `delivery_date_status.html` (available/unavailable + rate/reason) and `delivery_date_error.html` (user-friendly validation alerts).
- Implemented `POST /checkout/check-delivery` in `app/routes/checkout.py`: rejects past dates and invalid input with error partial; calls `KaprukaService.check_delivery` for valid requests.
- `render_delivery_date` / `render_delivery_date_status` / `render_delivery_date_error` in `app/templating.py`.
- Tests: `test_delivery_date.py` (zoneinfo, HTMX attrs, partials); `test_delivery_date_flow.py` integration (past date → error HTML, no MCP; valid date → availability partial).
Files: lib/utils/{timezone,__init__}.py, templates/checkout/{delivery_date,delivery_date_status,delivery_date_error}.html, app/{routes/checkout,templating}.py, tests/unit/test_delivery_date.py, tests/integration/test_delivery_date_flow.py, prd.json
Next: PRD-064 Delivery address and instructions form.

## PRD-064 — Delivery address and instructions form
- Added `lib/checkout/delivery.py`: `DeliveryFormValues` dataclass + `parse_delivery_form` validating via Kapruka `Delivery` Pydantic model (address 3–250, instructions max 250).
- Added `templates/checkout/delivery_form.html`: address, city (debounced autocomplete), location_type select, date (Colombo min), optional instructions; `hx-post /checkout/validate-delivery` with outerHTML swap preserving values.
- Added `templates/checkout/delivery_field_error.html`: per-field HTMX `hx-swap-oob="innerHTML"` error fragments.
- Implemented `POST /checkout/validate-delivery` in `app/routes/checkout.py`; `render_delivery_form` / `render_delivery_field_error` / `render_delivery_form_validation_response` in `app/templating.py`.
- Tests: `test_delivery_form.py` (structure, Pydantic bounds, OOB fragments); `test_delivery_form_flow.py` integration (short address → OOB error + city/instructions preserved; valid form → success marker).
Files: lib/checkout/{__init__,delivery}.py, templates/checkout/{delivery_form,delivery_field_error}.html, app/{routes/checkout,templating}.py, tests/unit/test_delivery_form.py, tests/integration/test_delivery_form_flow.py, prd.json
Next: PRD-065 Recipient name and phone validation.

## PRD-065 — Recipient name and phone validation
- Added `_SL_MOBILE_PHONE` regex and `validate_phone` field validator on `Recipient` in `lib/kapruka/types.py` (+9477XXXXXXX E.164 and 077XXXXXXX local).
- Added `lib/checkout/recipient.py`: `RecipientFormValues` + `parse_recipient_form` via Kapruka `Recipient` model.
- Added `templates/checkout/recipient_form.html` (name 1–80, phone tel input) and `recipient_field_error.html` (HTMX OOB fragments).
- Implemented `POST /checkout/validate-recipient`; `render_recipient_form` / `render_recipient_field_error` / `render_recipient_form_validation_response` in `app/templating.py`.
- Tests: `test_recipient_form.py` (parametrized valid/invalid phones, OOB fragments); `test_recipient_form_flow.py` integration (invalid phone preserves name; valid → success marker).
Files: lib/kapruka/types.py, lib/checkout/{recipient,__init__}.py, templates/checkout/{recipient_form,recipient_field_error}.html, app/{routes/checkout,templating}.py, tests/unit/test_recipient_form.py, tests/integration/test_recipient_form_flow.py, prd.json
Next: PRD-066 Sender form with anonymous toggle.

## PRD-066 — Sender form with anonymous toggle
- Added `lib/checkout/sender.py`: `SenderFormValues` + `parse_sender_form` via Kapruka `Sender` model (name 1–80, anonymous bool; no email).
- Added `templates/checkout/sender_form.html`: name input, anonymous checkbox, Alpine `x-model` gift-card preview (`Anonymous` vs entered name); `sender_field_error.html` OOB fragments.
- Implemented `POST /checkout/validate-sender`; `render_sender_form` / `render_sender_field_error` / `render_sender_form_validation_response` in `app/templating.py`.
- Tests: `test_sender_form.py` (HTMX attrs, Sender schema fields only, Alpine preview binding, Pydantic bounds); `test_sender_form_flow.py` integration (empty name OOB + anonymous preserved; valid with/without checkbox).
Files: lib/checkout/{sender,__init__}.py, templates/checkout/{sender_form,sender_field_error}.html, app/{routes/checkout,templating}.py, tests/unit/test_sender_form.py, tests/integration/test_sender_form_flow.py, prd.json
Next: PRD-067 Checkout sub-graph state machine definition.

## PRD-067 — Checkout sub-graph state machine definition
- Added `graphs/checkout_state.py`: `CheckoutState` TypedDict with `current_step`, `target_step`, `action`, form accumulators (cart, delivery, recipient, sender), `step_valid`, `validation_errors`; `resolve_navigation` blocks forward skips; `CHECKOUT_STEP_ORDER` matches PRD sequence.
- Added `graphs/nodes/checkout_steps.py`: per-step validation gates (Redis cart hydrate, city/date/recipient/sender Pydantic checks, review requires all prior steps + full Delivery); optional Kapruka MCP city/date checks when service injected.
- Added `graphs/checkout_graph.py`: `build_checkout_graph` / `get_checkout_graph` with `CheckoutGraphDeps` (redis, kapruka, client_ip); START routes to current step node only — one step per invoke.
- Tests: `tests/unit/test_checkout_graph.py` (cart→review jump blocked, sequential advance, empty cart, back navigation, Redis cart load, navigation matrix).
Files: graphs/{checkout_state,checkout_graph}.py, graphs/nodes/checkout_steps.py, graphs/__init__.py, tests/unit/test_checkout_graph.py, prd.json
Next: PRD-068 Checkout sub-graph integration with main graph.

## PRD-068 — Checkout sub-graph integration with main graph
- Added `graphs/nodes/run_checkout_graph.py`: hydrates Redis cart into `initial_checkout_state`, invokes checkout sub-graph, maps result to `checkout_state` + `tool_results.checkout`.
- Updated `route_after_analyze_intent`: `intent=checkout` routes to `run_checkout_graph` (not `call_mcp_tools`); tracking still skips HybridRAG.
- Wired `run_checkout_graph` node in `shopping_graph.py`; `ShoppingGraphDeps.redis_client` injected from `build_shopping_graph_deps`.
- `analyze_intent`: deterministic `PROCEED_CHECKOUT_MESSAGE` from cart drawer skips Gemini.
- `generate_response`: checkout branch renders assistant reply from checkout tool_results without LLM.
- Cart drawer: proceed button + `cart-drawer.js` `proceedToCheckout()` submits chat form.
- Tests: `test_run_checkout_graph.py`, `test_checkout_graph_integration.py` (Redis cart hydrate, proceed trigger, node order).
Files: graphs/{checkout_constants,nodes/run_checkout_graph,shopping_graph,nodes/{analyze_intent,generate_response,retrieve_hybrid_context}}, lib/chat/deps.py, templates/checkout/cart_partial.html, static/js/cart-drawer.js, tests/{unit,integration}/test_*.py, prd.json
Next: PRD-069 Checkout review step with Pro model.

## PRD-069 — Checkout review step with Pro model
- Added `lib/checkout/review.py`: `CheckoutReviewContext` + `review_context_from_checkout_state` for cart/delivery/recipient/sender summary data.
- Added `templates/checkout/review.html`: order review partial with cart lines, subtotal, delivery, recipient, sender/gift message sections (`data-testid` markers).
- Review step in `checkout_steps.py` renders `response_html` via `render_checkout_review` after successful review validation.
- `run_checkout_graph` passes full checkout fields + `review_html` in tool_results payload; sets `model_tier=pro` when `current_step=review`.
- `generate_response`: checkout review branch calls Gemini 2.5 Pro via `select_model` (checkout_state=review), embeds review partial in `message_assistant.html` `checkout_review_html` slot.
- Tests: `test_checkout_review.py` (template sections, review step HTML, model_tier pro, model_router); `test_generate_response` Pro model + embedded summary.
Files: lib/checkout/review.py, templates/checkout/review.html, templates/chat/message_assistant.html, app/templating.py, graphs/nodes/{checkout_steps,run_checkout_graph,generate_response}.py, tests/unit/{test_checkout_review,test_generate_response}.py, prd.json
Next: PRD-070 kapruka_create_order integration.

## PRD-070 — kapruka_create_order integration
- Added `finalize` checkout step after `review` in `CHECKOUT_STEP_ORDER`; fixed `PRE_REVIEW_STEPS` tuple so `all_steps_before_review_valid` no longer requires review before review validation.
- Added `lib/checkout/order.py`: `build_create_order_from_checkout` maps Redis cart rows + checkout state to Kapruka Recipient/Delivery/Sender/CartItem args.
- Added `lib/redis/order.py`: `store_pending_order` / `get_pending_order` persist `order_ref` and `expires_at` under `session:{id}:order_*` keys (7-day TTL).
- `execute_finalize_step` in `checkout_steps.py` calls `KaprukaService.create_order`, stores Redis metadata, returns `checkout_url` / `order_ref` / `expires_at` / `order_summary` in state.
- `run_checkout_graph` passes order fields in checkout tool_results; `generate_response` surfaces checkout URL message on finalize step.
- Tests: `test_checkout_order.py`, `test_redis_order.py`, `tests/integration/test_checkout_finalize.py` (mocked MCP + Redis persistence).
Files: lib/checkout/order.py, lib/redis/order.py, graphs/{state,checkout_state}.py, graphs/nodes/{checkout_steps,run_checkout_graph,generate_response}.py, tests/{unit/test_checkout_order,unit/test_redis_order,integration/test_checkout_finalize}.py, tests/unit/{test_checkout_graph,test_checkout_review}.py, prd.json
Next: PRD-071 Click-to-pay countdown UI component.

## PRD-071 — Click-to-pay countdown UI component
- Added `lib/checkout/payment.py`: `PaymentCtaContext`, `payment_cta_from_finalize`, and countdown helpers (`parse_expires_at_iso`, `format_countdown_mm_ss`, `is_countdown_warning`).
- Added `templates/checkout/payment_cta.html`: order_ref, grand_total, primary CTA (`target="_blank"`), Alpine `paymentCountdown` MM:SS timer with under-10-min warning and expired state.
- Added `static/js/payment-countdown.js`; loaded from `templates/chat/index.html` before deferred Alpine.
- `checkout_steps` renders payment CTA on finalize; `run_checkout_graph` exposes `payment_cta_html`; `generate_response` embeds CTA in `message_assistant.html` `checkout_payment_html` slot.
- Tests: `test_payment_cta.py`, `test_payment_countdown_js.py`, `test_payment_cta_browser.py` (warning + expired); integration finalize asserts payment partial.
Files: lib/checkout/payment.py, templates/checkout/payment_cta.html, static/js/payment-countdown.js, app/templating.py, graphs/nodes/{checkout_steps,run_checkout_graph,generate_response}.py, templates/chat/{message_assistant,index}.html, tests/unit/{test_payment_cta,test_payment_countdown_js,test_payment_cta_browser}.py, tests/integration/test_checkout_finalize.py, prd.json
Next: PRD-072 Order tracking chat flow.

## PRD-072 — Order tracking chat flow
- Added `lib/checkout/tracking.py`: `extract_order_number` regex for post-payment IDs (e.g. VIMP34456CB2), rejects pre-payment `ORD-*` refs; `tracking_output_from_tool_results` parses MCP payload.
- Added `templates/checkout/tracking_status.html`: status badge, delivery/recipient summary, vertical progress timeline (`data-testid="tracking-timeline"`).
- `call_mcp_tools`: tracking intent invokes `kapruka_track_order` when order number present; `TRACK_ORDER_TOOL` added to supported explicit tool calls.
- `generate_response`: tracking branch renders deterministic reply + embedded tracking partial; prompts for order number without LLM when missing.
- `message_assistant.html` gains `tracking_status_html` slot; `render_tracking_status` in `app/templating.py`.
- Tests: `test_tracking_status.py` (extraction, timeline HTML, MCP invoke, generate_response embed); updated `test_call_mcp_tools.py` tracking cases.
Files: lib/checkout/tracking.py, templates/checkout/tracking_status.html, templates/chat/message_assistant.html, graphs/nodes/{call_mcp_tools,generate_response}.py, app/templating.py, tests/unit/{test_tracking_status,test_call_mcp_tools}.py, prd.json
Next: PRD-073 Exception middleware for MCP errors.

## PRD-073 — Exception middleware for MCP errors
- Added `app/middleware/errors.py`: `register_exception_handlers` maps KaprukaValidationError→400, KaprukaNotFoundError→404, KaprukaRateLimitError/RateLimitExceeded→429 (Retry-After), KaprukaError→502, Exception→500 generic HTML via `templates/partials/error_banner.html`.
- HTMX clients (`HX-Request: true`) receive HTML partials with `data-error-code`; JSON clients get `{error_code, message}`. Friendly message map for known MCP codes; lazy template import avoids startup circular import.
- `render_error_banner()` in `app/templating.py`; wired in `create_app()` after routes/static mount.
- Tests: `tests/unit/test_error_middleware.py` (400 HTML not JSON for validation, 429/404/502 paths, generic 500 without leak).
Files: app/middleware/{__init__,errors}.py, app/{main,templating}.py, templates/partials/error_banner.html, tests/unit/test_error_middleware.py, prd.json
Next: PRD-074 429 rate limit alert banner with countdown.

## PRD-074 — 429 rate limit alert banner with countdown
- Extended `app/middleware/errors.py`: `_rate_limit_response` maps KaprukaRateLimitError and RateLimitExceeded to dedicated HTML banner (429 + Retry-After); JSON clients receive `retry_after_seconds` in payload.
- Added `templates/partials/rate_limit_banner.html`: amber alert with `data-retry-after`, Alpine `rateLimitCountdown`, MM:SS display, fade-out auto-dismiss when window resets.
- Added `static/js/rate-limit-countdown.js`; loaded globally from `templates/base.html` before deferred Alpine.
- `render_rate_limit_banner()` in `app/templating.py`.
- Tests: `test_rate_limit_banner_renders_retry_after_countdown`, updated 429 middleware tests for `data-retry-after`; `test_rate_limit_countdown_js.py`.
Files: app/middleware/errors.py, app/templating.py, templates/{partials/rate_limit_banner,base}.html, static/js/rate-limit-countdown.js, tests/unit/{test_error_middleware,test_rate_limit_countdown_js}.py, prd.json
Next: PRD-075 NetworkX community detection background worker.

## PRD-075 — NetworkX community detection background worker
- Added `lib/analytics/networkx_worker.py`: fetches `CO_PURCHASED_WITH` Product edges; falls back to synthesizing ProductType pairs sharing a parent Category; builds weighted NetworkX graph; runs `louvain_communities` via `asyncio.to_thread`; MERGEs bidirectional `RECOMMENDS` with score/community_id/source in batches.
- `NetworkXCommunityWorker` exposes `run_once()`, `start()` / `stop()` periodic asyncio loop (default 3600s interval); lifespan wiring deferred until Neo4j client is connected in app startup (PRD-081).
- Mypy override for `networkx.*` missing stubs in `pyproject.toml`.
- Tests: `tests/unit/test_networkx_worker.py` (Louvain partition, co-purchase vs category-proximity fallback, RECOMMENDS persist, worker start/stop).
Files: lib/analytics/{__init__,networkx_worker}.py, tests/unit/test_networkx_worker.py, pyproject.toml, prd.json
Next: PRD-076 Optional cuGraph CUDA Dockerfile stage.

## PRD-076 — Optional cuGraph CUDA Dockerfile stage
- Added `Dockerfile.cuda`: RAPIDS CUDA 12.8 + Python 3.12 base, installs app deps and `cugraph-cu12` via NVIDIA PyPI index; dev-only, not used for Cloud Run.
- Added `lib/analytics/cugraph_optional.py`: lazy `importlib` load of cuGraph/cuDF when CUDA driver visible; `detect_louvain_communities_gpu()` mirrors NetworkX Louvain output shape; `preferred_backend()` returns `"cugraph"` or `"networkx"`.
- README documents NetworkX as production path and optional GPU Docker build for local dev.
- Mypy overrides for optional `cugraph`/`cudf` imports; exports wired in `lib/analytics/__init__.py`.
- Tests: `tests/unit/test_cugraph_optional.py` (CUDA detection, import fallback, mocked GPU Louvain partition).
Files: Dockerfile.cuda, lib/analytics/{__init__,cugraph_optional}.py, tests/unit/test_cugraph_optional.py, README.md, pyproject.toml, prd.json
Next: PRD-077 Ragas evaluation golden dataset.

## PRD-077 — Ragas evaluation golden dataset
- Added `evals/golden_dataset.json`: 24 golden cases (12 discovery, 8 checkout guidance, 4 tracking) with `user_query`, `expected_tools`, `reference_answer`; covers birthday cake search, Colombo flower delivery check, and VIMP order tracking per PRD.
- Added `evals/golden_dataset.py`: Pydantic v2 `GoldenCase` / `GoldenDataset` schema, `KNOWN_MCP_TOOLS` allowlist for all 7 Kapruka MCP tools, `load_golden_dataset()` + cached `get_golden_dataset()`.
- Tests: `tests/unit/test_golden_dataset.py` (fixture loader, ≥20 cases, scenario coverage, PRD anchor cases, unknown-tool rejection).
Files: evals/{__init__,golden_dataset,golden_dataset.json}, tests/unit/test_golden_dataset.py, prd.json
Next: PRD-078 Ragas eval script and CI mock MCP fixture.

## PRD-078 — Ragas eval script and CI mock MCP fixture
- Added `tests/fixtures/mcp_mock.py`: `MockMCPHttpClient` + `mock_mcp_response()` with deterministic JSON for all 7 Kapruka MCP tools; query/order/city params reflected in payloads.
- Added `evals/ragas_eval.py`: `run_ragas_eval()` / `run_full_ragas_eval()` run shopping graph per golden case with mock MCP + intent-specific Gemini mock; score context_precision, answer_relevancy, faithfulness via Ragas; `--ci` gate enforces context_precision ≥ 0.7 (24-case run ~0.96).
- Added `evals/ragas_ci_llm.py`: `CiRagasChatModel` deterministic judge LLM for CI (no API keys).
- Added `.github/workflows/ci.yml` `ragas-eval` job: `python -m evals.ragas_eval --ci`.
- Tests: `tests/unit/test_mcp_mock.py`, `tests/unit/test_ragas_eval.py` (graph row collection, full golden dataset threshold).
Files: tests/fixtures/mcp_mock.py, evals/{ragas_eval,ragas_ci_llm,__init__}.py, .github/workflows/ci.yml, tests/unit/{test_mcp_mock,test_ragas_eval}.py, prd.json
Next: PRD-079 Ruff and mypy CI gate.

## PRD-079 — Ruff and mypy CI gate
- Extended `.github/workflows/ci.yml` with `lint-and-test` job: Python 3.12 matrix, `ruff check`, `ruff format --check`, `mypy app/ lib/ graphs/`, `pytest tests/unit -q` on push/PR (unit-only; integration tests excluded).
- Kept existing `ragas-eval` job from PRD-078; both jobs share `pip install -e ".[dev]"` pattern.
- Verified locally: ruff, mypy, and 441 unit tests pass.
Files: .github/workflows/ci.yml, prd.json
Next: PRD-080 Multi-stage production Dockerfile.

## PRD-080 — Multi-stage production Dockerfile
- Added `Dockerfile`: builder stage (`python:3.12-slim`) venv `pip install .`, Tailwind standalone compile to `static/css/app.css`, copies `templates/` + `static/` into site-packages (paths resolve via `app/*` → `parent.parent`); runtime stage non-root `app` user, `PORT=8080`, `gunicorn app.main:app -k uvicorn.workers.UvicornWorker --bind 0.0.0.0:8080`.
- Added `.dockerignore` excluding tests, evals, secrets, dev CUDA image, and generated `bin/`.
- Tests: `tests/unit/test_dockerfile.py` (multi-stage contract); verified `docker build` + container `/health` → `{"status":"ok"}` on port 8080.
- README: production `docker build` / `docker run` / `curl /health` snippet.
Files: Dockerfile, .dockerignore, tests/unit/test_dockerfile.py, README.md, prd.json
Next: PRD-081 Aggregated health check endpoint.

## PRD-081 — Aggregated health check endpoint
- Added `lib/health/aggregator.py`: Pydantic `AggregatedHealthResponse` probes Redis `ping()`, Neo4j `health_check()`, Zep `health_check()`, and MCP `list_categories(depth=1)`; returns `healthy` + 200 when all up, `degraded` + 503 otherwise.
- `GET /health` in `app/routes/health.py` returns JSON `{status, services: {redis, neo4j, zep, mcp}}` with per-service `up`/`down`.
- Extended `app/lifespan.py`: optional connect for Redis, Neo4j, Zep, and Kapruka MCP on startup (`_connect_optional` logs and sets None on failure); closes MCP on shutdown.
- Tests: `tests/unit/test_health.py` (aggregator + endpoint schema, 503 degraded path); updated `test_main.py`, `test_dependencies.py` lifespan coverage.
Files: lib/health/{__init__,aggregator}.py, app/{routes/health,lifespan}.py, tests/unit/{test_health,test_main,test_dependencies}.py, README.md, prd.json
Next: PRD-082 Gunicorn Uvicorn production entrypoint.

## PRD-082 — Gunicorn Uvicorn production entrypoint
- Added `gunicorn.conf.py`: `workers = 2 * cpu_count + 1`, `worker_class = uvicorn.workers.UvicornWorker`, `bind` from `PORT` (default 8080), `timeout=120` for SSE streams, `graceful_timeout=30`, `keepalive=5` for Cloud Run drain/LB reuse.
- Updated `Dockerfile` CMD to `gunicorn -c gunicorn.conf.py app.main:app`; copies config into runtime image with comment documenting entrypoint.
- README documents local `gunicorn -c gunicorn.conf.py` equivalent.
- Tests: `tests/unit/test_gunicorn_conf.py` (config values, PORT bind, subprocess serves `/health` JSON); updated `test_dockerfile.py` for new CMD contract.
- Verified: ruff, mypy, 452 unit tests pass; gunicorn subprocess integration test reaches `/health` (200 or 503 without live backends).
Files: gunicorn.conf.py, Dockerfile, README.md, tests/unit/{test_gunicorn_conf,test_dockerfile}.py, prd.json
Next: PRD-083 Local .env bootstrap via gcloud CLI.

## PRD-083 — Local .env bootstrap via gcloud CLI
- Added `scripts/bootstrap_env.sh`: reads `GCP_PROJECT_ID` and `GCP_LOCATION` from gcloud config; creates or reuses a `generativelanguage.googleapis.com` API key via `gcloud services api-keys`; generates `SESSION_SECRET` with `secrets.token_urlsafe(48)`; writes `REDIS_URL` and `KAPRUKA_MCP_URL` defaults; leaves Neo4j and Zep as documented placeholders from `.env.example`.
- `BOOTSTRAP_ENV_FILE` override for tests; `--force` to overwrite existing `.env`; chmod 600 on output.
- Confirmed `.gitignore` already ignores `.env` / `.env.*` with `!.env.example`.
- Tests: `tests/unit/test_bootstrap_env.py` (gitignore contract, script contract, mocked gcloud run + Settings load, overwrite guard).
- README quick start documents `./scripts/bootstrap_env.sh`.
Files: scripts/bootstrap_env.sh, tests/unit/test_bootstrap_env.py, README.md, prd.json
Next: PRD-084 Google Cloud Run deployment with gcloud.

## PRD-084 — Google Cloud Run deployment with gcloud
- Added `scripts/deploy_cloud_run.sh`: enables required GCP APIs, creates Artifact Registry repo if missing, builds via `gcloud builds submit` (documents local `docker build`/`push` alternative), deploys with `--vpc-connector`, `--vpc-egress=private-ranges-only`, `--set-secrets` for all runtime credentials, `--set-env-vars` for GCP_PROJECT_ID/GCP_LOCATION/KAPRUKA_MCP_URL, `--min-instances=0`; supports `--dry-run` and `--skip-build`.
- Added `docs/DEPLOY.md`: step-by-step gcloud walkthrough (Artifact Registry, Cloud Build / docker push, VPC connector for Memorystore, Secret Manager setup + IAM, env var checklist, manual `gcloud run deploy`, verify `/health`).
- README links to deploy script and DEPLOY.md.
- Tests: `tests/unit/test_deploy_cloud_run.py` (script + doc contract, dry-run output).
Files: scripts/deploy_cloud_run.sh, docs/DEPLOY.md, tests/unit/test_deploy_cloud_run.py, README.md, prd.json
Next: PRD-085 Playwright E2E smoke tests.

## PRD-085 — Playwright E2E smoke tests
- Added `tests/e2e/`: `e2e_app.py` (fakeredis + MockMCPHttpClient + mock Gemini), `e2e_server.py` (uvicorn on :8080), `conftest.py` (session server + pytest-playwright `base_url`), `test_smoke.py` (chat load, search→product card, cart drawer add, checkout delivery form without create_order).
- Extended `tests/fixtures/mcp_mock.py` with `call_log`; E2E-only `GET /e2e/mcp-calls` asserts `kapruka_create_order` never invoked.
- `pyproject.toml`: `base_url = http://localhost:8080`, `e2e`/`browser` markers; CI job `e2e-smoke` runs `playwright install chromium` + `pytest tests/e2e`.
- Bug fixes found during E2E: `chat-sse.js` strips HTMX attrs before htmx init (inline prep script), processes only new SSE fragments; `cart-drawer.js` reads live `#cart-panel` count after outerHTML swap + delegated proceed-checkout clicks; `cart_partial.html` drops Alpine `@click` on HTMX-swapped button.
- Tests: `tests/unit/test_e2e_app.py`; 4 E2E + 462 unit tests pass.
Files: tests/e2e/*, tests/fixtures/mcp_mock.py, tests/unit/test_e2e_app.py, tests/unit/test_cart_partial.py, static/js/{chat-sse,cart-drawer}.js, templates/chat/index.html, templates/checkout/cart_partial.html, pyproject.toml, .github/workflows/ci.yml, prd.json
Next: PRD-086 IaC bash script for GitHub Actions secrets and Cloud Run CI/CD.
